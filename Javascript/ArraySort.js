function decendingOrder() {
	var arr2 = [2, 3, 1, 4, 5, 6, 7]
	arr2.sort()
	console.log('Array Acending order ' + arr2);
	console.log('Array Decending order ' + arr2.reverse());
}
decendingOrder();
